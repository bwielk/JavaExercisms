class RailFenceCipher {

    private int numOfRows;

    public RailFenceCipher(int numOfRows){
        this.numOfRows = numOfRows;
    }

    public String getEncryptedData(String stringToConsume){
        char[][] rails = new char[numOfRows][stringToConsume.length()];
        for(char c = 0; c<stringToConsume.length(); c++){

        }
        String result = "";
        return result;
    }

    public String getDecryptedData(String stringToConsume){
        String result = "";
        return result;
    }
}
